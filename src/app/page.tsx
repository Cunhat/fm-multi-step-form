import Image from "next/image";

export default function Home() {
  return (
    <main className="flex min-h-screen flex-col items-center bg-mobile-background bg-top bg-no-repeat bg-contain sm:bg-none">
      <div className="fixed bottom-0 w-full h-[72px] bg-white sm:hidden"></div>
    </main>
  );
}
